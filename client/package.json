{
  "scripts": {
    "dev": "http-server -p 3000 --cors --watch",
    "lint": "eslint app/**/*.{js,html}",
    "lint:watch": "nodemon --watch app/**/*.{js,html} --exec \"npm.cmd run lint\"",
    "build": "babel src --out-dir dist"
  },
  "dependencies": {
    "angular": "^1.8.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.25.7",
    "@babel/core": "^7.25.8",
    "@babel/preset-env": "^7.25.8",
    "eslint": "^8.0.0",
    "eslint-plugin-angular": "^4.1.0",
    "http-server": "^14.1.1",
    "nodemon": "^3.1.7"
  }
}
